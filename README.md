# Academy-Top-C-sharp

#### Решения домашних заданий C#

## Homework 01

### Тема: "Введение в C# и платформу .NET"

**Задание:** Напишите программу, которая выводит на экран рецепт вашего любимого простого блюда или напитка (например, бутерброда, чая, салата). Программа должна последовательно отобразить:
- Название блюда/напитка.
- Заголовок "Ингредиенты:".
- Список ингредиентов (каждый на новой строке).
- Заголовок "Приготовление:".
- Краткую последовательность шагов приготовления (каждый на новой строке).

**Требования к выполнению:**
- Приложение должно быть создано на основе шаблона "Консольное приложение"
- После вывода всей информации программа должна ожидать нажатия клавиши Enter от пользователя
- В исходном коде должен присутствовать как минимум один комментарий, поясняющий любую часть вашей программы (например, описывающий назначение блока кода или конкретной строки)

## Homework 02

### Тема: "Переменные, базовые типы данных"

**Задание:** Напишите программу, которая вычисляет Индекс Массы Тела (ИМТ). Программа должна последовательно запросить у пользователя:
- Вес в килограммах (например, 75.5).
- Рост в метрах (например, 1.82).

После получения данных программа должна рассчитать ИМТ по формуле и вывести результат на экран.    
Формула для расчета ИМТ: ИМТ = Вес (в кг) / (Рост (в м) * Рост (в м))

**Требования к выполнению:**
- Приложение должно быть создано на основе шаблона "Консольное приложение"
- Результат должен быть выведен в понятном для пользователя виде с использованием интерполяции строк (через $"").
- В исходном коде должен присутствовать как минимум один осмысленный комментарий.

## Homework 03

### Тема: "Условные конструкции и ветвление логики"

**Задание:** Напишите программу "Улучшенный калькулятор", которая запрашивает у пользователя два числа и символ арифметической операции, а затем выводит результат этой операции.

Логика работы программы:
- Программа запрашивает у пользователя первое число.
- Программа запрашивает у пользователя второе число.
- Программа просит пользователя ввести символ одной из четырех операций: +, -, *, /.
- В зависимости от введенного символа программа выполняет соответствующую операцию над числами.
- Программа выводит результат на экран.
- Особые случаи:
  - Программа должна корректно обрабатывать попытку деления на ноль, выводя соответствующее сообщение об ошибке.
  - Если пользователь вводит неверный символ операции (любой, кроме +, -, *, /), программа должна сообщить об этом.

**Требования к выполнению:**
- Приложение должно быть создано на основе шаблона "Консольное приложение"
- Для выбора операции используйте конструкцию switch (рекомендуется) или лестницу if, else if, else.
- Результат должен быть выведен в понятном для пользователя виде с использованием интерполяции строк.

## Homework 04

### Тема: "Циклы. Повторяющиеся действия"

**Задание:** Модифицировать игру "Угадай число" из практической работы, добавив в нее следующие улучшения:
- Ограниченное количество попыток: У пользователя должно быть ограниченное количество попыток (например, 7) чтобы угадать число.
- Два исхода игры:
  - Если пользователь угадывает число за отведенное количество попыток, он выигрывает.
  - Если попытки заканчиваются, а число не угадано, пользователь проигрывает, и программа должна сообщить, какое число было загадано.
- Возможность сыграть снова: после окончания каждой партии (неважно, победа или поражение) программа должна спрашивать у пользователя, хочет ли он сыграть еще раз. Если пользователь вводит "да" (или "yes"), игра должна начаться заново с новым загаданным числом. Если он вводит что-либо другое, программа должна завершиться.

**Требования к выполнению:**
- Для основного игрового цикла (который позволяет играть снова и снова) используйте цикл do-while.
- Обязательно используйте класс Random для генерации нового числа в начале каждой новой игры.
- В конце, после отказа пользователя играть снова, программа должна ожидать нажатия клавиши Enter перед завершением.

## Homework 05

### Тема: "Методы. Организация кода"

**Задание:** Вспомните программу *Улучшенный калькулятор*, которую вы писали в домашнем задании к уроку 3. Весь ее код, скорее всего, был написан в одном месте (внутри метода Main или на верхнем уровне).

Ваша задача — выполнить **рефакторинг** этой программы, разбив ее на логические, переиспользуемые методы. Основная функциональность калькулятора должна остаться прежней, но его внутренняя структура должна кардинально измениться.

**Требования к выполнению:**
- Программа должна быть написана с использованием **классической структуры**.
- **Метод Main** должен быть **чистым**: он не должен содержать сложной логики, а в основном состоять из вызовов других, вспомогательных методов. Он играет роль дирижера программы.
- Вы должны создать и использовать как **минимум три** вспомогательных метода.

Внешнее поведение программы не должно измениться. Результат взаимодействия с пользователем должен остаться таким же, как и в домашнем задании к уроку 3.

## Homework 06

### Тема: "Массивы. Хранение коллекций данных"

**Задание:** Написать программу, которая "переворачивает" массив. Программа должна создать массив с заранее определенными значениями, а затем создать второй массив, который будет содержать те же элементы, но в обратном порядке.

**Логика работы:**
1. В методе Main создается исходный массив с заранее определенными значениями (например, *int[] originalArray = { 10, 20, 30, 40, 50 }*;).
2. Программа выводит исходный массив на экран в одну строку.
3. Программа создает новый массив такого же размера, что и исходный.
4. С помощью **цикла for** программа заполняет новый массив элементами из исходного, но в обратном порядке. (Подсказка: вам понадобятся два счетчика или одна хитрая формула для вычисления индексов).
5. Программа выводит перевернутый массив на экран, также в одну строку.

**Требования к выполнению:**
- Программа должна быть написана с использованием **классической структуры**.
- Для вывода массивов на экран используйте циклы (for или foreach) и Console.Write(), чтобы элементы выводились в одну строку через пробел.
- Для заполнения перевернутого массива используйте цикл for, так как вам потребуется работать с индексами обоих массивов.

## Homework 07

### Тема: "Введение в классы и объекты. Создание собственных типов"

**Задание:** Создайте программу для работы с геометрической фигурой "Прямоугольник". Для этого вам необходимо спроектировать и реализовать **класс Rectangle**.

**Требования к выполнению:**
- Добавьте поля: внутри класса Rectangle должны быть два публичных (public) поля:
  - double width (ширина)
  - double height (высота)
- Добавьте методы: Внутри класса Rectangle должны быть два публичных метода, которые возвращают результат:
  - double GetArea(): этот метод должен вычислять площадь прямоугольника (ширина * высота) и возвращать результат.
  - double GetPerimeter(): этот метод должен вычислять периметр прямоугольника (2 * (ширина + высота)) и возвращать результат.
- Используйте класс в Main: Внутри метода Main выполните следующие действия:
  - Создайте как минимум два разных объекта (экземпляра) класса Rectangle.
  - Задайте значения для полей width и height каждого объекта.
  - Для каждого объекта вызовите методы GetArea() и GetPerimeter().
  - Сохраните возвращенные значения в переменные.
  - Выведите на экран информацию о каждом прямоугольнике, включая его размеры, площадь и периметр.

## Homework 08

### Тема: "Свойства и конструкторы. Контроль и инициализация объектов"

**Задание:** Вспомните *класс Student* из практической работы к уроку 7. Как и Rectangle, он имел публичные поля, что позволяло создавать студентов с некорректными данными (например, с пустым именем или отрицательным возрастом).

Ваша задача — выполнить рефакторинг класса Student, чтобы сделать его надежным, безопасным и современным.

**Требования к выполнению:**
- **Замените все публичные поля** (firstName, lastName, age) на публичные автоматически реализуемые свойства (FirstName, LastName, Age).
- **Улучшите свойство Age**: Преобразуйте автоматическое свойство Age в свойство с полной реализацией (с приватным резервным полем _age). Добавьте в аксессор set логику валидации: возраст должен быть в разумных пределах (например, от 6 до 100 лет). Если присваиваемое значение некорректно, оно не должно сохраняться, а в консоль должно выводиться сообщение об ошибке.
- **Добавьте конструктор**: Создайте публичный конструктор, который принимает в качестве параметров имя, фамилию и возраст студента. Конструктор должен инициализировать соответствующие свойства.
- **Добавьте вычисляемое свойство**: Создайте новое публичное свойство только для чтения с именем FullName (тип string). Аксессор get этого свойства должен возвращать объединенные имя и фамилию студента через пробел (например, "Иван Петров").

## Homework 09

### Тема: "Статические члены, типы-значения и ссылочные типы"

**Задание:** Представьте, что вы разрабатываете небольшой модуль для информационной системы университета. Одна из ключевых задач — **вести учет студентов**. Система должна не только хранить информацию о каждом студенте, но и в любой момент времени знать общее количество зарегистрированных в системе студенческих профилей.

Кроме основной информации, профиль каждого студента должен содержать данные о его любимой книге — простое сочетание названия и автора.

Вам необходимо создать консольную программу, которая эмулирует работу этой системы: создает несколько профилей, отслеживает их общее количество и, что самое важное, наглядно демонстрирует фундаментальную разницу в поведении при работе с профилем студента (как со сложной, уникальной сущностью) и с информацией о книге (как с простой структурой данных).

**Что должна делать программа?** Ваше итоговое приложение должно реализовывать следующую логику:

**Определить два пользовательских типа данных:**
- **Book:** должен быть типом-значением (struct), так как логически представляет собой простую, неделимую единицу данных. Он должен содержать свойства для названия и автора.
- **Student:** должен быть ссылочным типом (class), так как представляет собой сложный, уникальный объект. Он должен содержать свойства для хранения имени студента и его любимой книги (Book).

**Реализовать статический счетчик:** Класс Student должен содержать механизм для подсчета всех созданных объектов. Этот счетчик должен быть общим для всего класса, а его значение должно быть доступно для чтения извне, но не для прямого изменения.

**Продемонстрировать основной сценарий в методе Main:**
1. Показать начальное количество студентов в системе.
2. Создать несколько экземпляров класса Student, корректно инициализировав их данные.
3. После создания каждого студента показывать обновленное общее количество.
4. Провести эксперимент с копированием:    
   a. Скопировать одну из переменных-студентов в новую переменную.    
   b. Скопировать структуру-книгу из этого же студента в отдельную переменную.    
   c. Внести изменения в копии (изменить имя у скопированного студента и название у скопированной книги).    
   d. Вывести на экран данные оригинального студента и его книги, чтобы наглядно показать, какие из исходных данных изменились, а какие   остались прежними, и объяснить почему.

## Homework 10

### Тема: "Работа со строками и StringBuilder. Эффективная обработка текста"

**Задание:** Вы получили "грязные" данные в виде одной длинной строки, где имена и фамилии пользователей перемешаны с лишними пробелами и записаны в разном регистре. Ваша задача — написать программу, которая очищает эти данные и формирует из них аккуратный, отформатированный список.

**Исходная строка:**

" иванов иван,петров петр, сидорова Анна, бобров БОРИС "

**Что должна делать программа?** Разделить исходную строку на отдельные пары "фамилия имя".

**Для каждой пары:**
- Очистить от лишних пробелов в начале и в конце.
- Отформатировать так, чтобы первая буква фамилии и первая буква имени были заглавными, а остальные — строчными.

**Собрать** отформатированные имена в одну итоговую строку, где каждая ФИО находится на новой строке и пронумерована.

## Homework 11

### Тема: "Обобщения (Generics) и List<T>. Гибкие коллекции"

**Задание:** Создайте *продвинутый список задач*. В отличие от практической работы, где вы хранили простые строки, в этом задании каждая задача будет полноценным объектом со своим состоянием.

Программа должна управлять списком задач, где у каждой задачи есть не только описание, но и статус выполнения (выполнена или нет).

**Логика работы программы:**
1. **Класс TaskItem**: сначала необходимо определить класс для представления одной задачи. У него должны быть свойства:    
   a. Description (тип string): Текст задачи.    
   b. IsDone (тип bool): Статус выполнения.
2. **Основная программа:**
   a. В методе Main создается List<TaskItem> для хранения всех объектов-задач.    
   b. Программа работает в бесконечном цикле, предлагая пользователю меню:    
      i. **Добавить задачу:** Программа запрашивает описание новой задачи, создает новый объект TaskItem (со статусом IsDone = false по умолчанию) и добавляет его в список.    
      ii. **Посмотреть задачи:** Программа выводит пронумерованный список всех задач. Статус каждой задачи должен быть наглядно отображен, например, [X] для выполненных и [ ] для невыполненных.    
      iii. **Отметить задачу как выполненную:** Программа показывает список задач и просит пользователя ввести номер задачи, которую нужно отметить. После ввода номера, у соответствующего объекта в списке свойство IsDone должно измениться на true.    
      iv. **Выйти:** Программа завершает свою работу.

## Homework 12

### Тема: "Лямбда-выражения и делегаты. Код как данные"

**Задание:** Создайте программу, которая демонстрирует мощь передачи поведения в качестве параметра. Вам нужно написать один-единственный метод для вычислений, который сможет выполнять любую математическую операцию над двумя числами. Сама операция (сложение, вычитание, умножение и т.д.) должна передаваться в этот метод в виде параметра-делегата.

**Что должна делать программа?** Ваше приложение должно запрашивать у пользователя два числа, а затем последовательно применять к ним различные математические операции, вызывая один и тот же универсальный метод, но с разными лямбда-выражениями.

## Homework 13

### Тема: "Введение в LINQ. Язык запросов к данным"

**Задание:** Вам предстоит написать программу для анализа списка студентов. Представьте, что у вас есть полный список студентов курса, и вам нужно быстро получить из него различные статистические выборки для отчетов, не прибегая к написанию сложных циклов и условий.

Выполните несколько аналитических задач:
1. **Найти хорошистов:** Первая задача — отфильтровать общий список и найти только тех студентов, чей средний балл находится в определенном диапазоне (например, от 75 до 90). Результатом должен быть вывод их имен и баллов.
2. **Сформировать простой список имен:** часто для отчетов нужны не все данные, а только что-то одно. Ваша программа должна уметь преобразовать сложную коллекцию объектов Student в простую коллекцию строк (string), содержащую только их полные имена.
3. **Отсортировать студентов по возрасту:** необходимо продемонстрировать возможность сортировки. Выведите полный список студентов, упорядоченный по их возрасту от самого младшего к самому старшему.
4. **Сформировать рейтинг:** это комплексная задача, которая должна быть решена одним последовательным запросом. Необходимо составить рейтинг лучших студентов, но только среди тех, кто младше 25 лет. Итоговый список должен быть отсортирован по убыванию среднего балла и представлен в виде строк формата "Имя - Средний балл".

## Homework 15

### Тема: "Словари и хэш-таблицы. Мгновенный поиск данных"

**Задание:**    
Напишите программу "Частотный словарь", которая анализирует текстовый фрагмент и подсчитывает, сколько раз в нем встречается каждое слово.    
Это классическая задача в программировании и анализе данных, которая идеально решается с помощью словаря, где ключом является само слово, а значением — количество его повторений.

**Логика работы программы:**
1. В программе задана исходная строка с текстом.
2. Программа очищает текст: приводит его к нижнему регистру и удаляет знаки препинания (точки, запятые), чтобы, например, "слово" и "слово." считались одинаковыми.
3. Очищенный текст разделяется на массив отдельных слов.
4. Программа создает пустой Dictionary<string, int>.
5. Используя цикл, программа перебирает массив слов. Для каждого слова:    
   a. Если такое слово уже есть в словаре (проверяется наличие ключа), то значение, соответствующее этому ключу, увеличивается на 1.    
   b. Если такого слова в словаре еще нет, оно добавляется как новый ключ со значением 1.    
6. После обработки всех слов программа выводит на экран содержимое словаря, показывая каждое уникальное слово и частоту его встречаемости.

## Homework 16

### Тема: "Асимптотическая сложность и структуры данных. Оцениваем эффективность кода"

**Задача:** Поиск пары чисел    
Представьте, что вам дали массив целых чисел и целевое число. Ваша задача — написать программу, которая находит в массиве два числа, сумма которых равна целевому числу.
**Пример:**
- Массив: { 3, 5, -4, 8, 11, 1, -1, 6 }
- Целевая сумма: 10
- Результат: Программа должна найти пару 11 и -1, так как 11 + (-1) = 10.

**Что должна делать программа**    
Ваше приложение должно содержать два разных статических метода, каждый из которых решает поставленную задачу, а также код в Main для демонстрации их работы.
